// Export types
export type { IEnvService, Session, Auth } from "./types";

// Export permissions at root level
export * from "./permissions";

// Note: Server and client exports are available via their respective subpaths:
// - @repo/auth/server
// - @repo/auth/client
// - @repo/auth/permissions
