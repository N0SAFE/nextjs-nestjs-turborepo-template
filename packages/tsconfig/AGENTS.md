# AGENTS.md — @repo/tsconfig

Shared TypeScript configurations.

## Rules
- Keep strict mode and modern targets unless project requirements change.
- Validate `tsc -b` and package type-checks after changes.
