/**
 * Fallback defaults for local / test usage.
 * **Never** rely on defaults in production.
 */

export const LOCAL_APP_FALLBACK = 'http://localhost:3000'
export const LOCAL_API_FALLBACK = 'http://localhost:3001'
export const LOCAL_DOC_FALLBACK = 'http://localhost:3020'

export const DEFAULT_API_PORT = 3001
export const DEFAULT_DOC_PORT = 3020
export const DEFAULT_WEB_PORT = 3000
